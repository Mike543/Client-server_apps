"""3. Определить, какие из слов «attribute», «класс», «функция», «type»
невозможно записать в байтовом типе."""

attr = b'attribute'
cls = b'класс'
func = b'функция'
typ = b'type'

# кириллицу нельзя записать в байтовом типе без использования encode